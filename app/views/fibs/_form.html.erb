<%= form_with(model: fib, local: true) do |form| %>
  <% if fib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fib.errors.count, "error") %> prohibited this fib from being saved:</h2>

      <ul>
      <% fib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :max_number %>
    <%= form.number_field :max_number, id: :fib_max_number %>
  </div>

  <div class="field">
    <%= form.label :sequence %>
    <%= form.text_field :sequence, id: :fib_sequence, disabled: true %>
  </div>

  <div class="field">
    <%= form.label :squares %>
    <%= form.number_field :squares, id: :fib_squares, disabled: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
